// globales.hpp
#ifndef GLOBALES_HPP
#define GLOBALES_HPP

#include <map>
#include <vector>

extern std::map<char, int> alfabeto;                     // Declaración de la variable global
extern std::vector<std::vector<int>> transponer;         // Declaración de la variable global
extern std::vector<std::vector<int>> sustituir;          // Declaración de la variable global
extern std::vector<int> insertar;                        // Declaración de la variable global
extern std::vector<int> deletear;                        // Declaración de la variable global

#endif // GLOBALES_HPP

